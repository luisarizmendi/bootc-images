# IdM Server Configuration File
# This file is sourced by installation scripts
# Environment variables in credentials.conf take precedence

# Realm and Domain
IPA_REALM="EXAMPLE.COM"
IPA_DOMAIN="example.com"

# Hostname (will be auto-detected if not set)
# IPA_HOSTNAME="idm.example.com"

# DNS Configuration
SETUP_DNS="yes"
DNS_FORWARDER="8.8.8.8"

# Note: Passwords should be set via environment variables
# in credentials.conf for security