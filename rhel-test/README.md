# rhel bootc image

This is a clean RHEL image with a simple first boot script. It has three versions, v2 adds 'tmux' package and v3 uses RHEL 10 instead RHEL 9.

Associated ISO is created based on v1 to show how upgrades work.
